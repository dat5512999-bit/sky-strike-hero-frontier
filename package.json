{
  "name": "sky-strike-phase-1",
  "version": "0.3.0",
  "private": true,
  "description": "純 HTML5 Canvas 縱向卷軸射擊遊戲 Phase 1",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/skins/SkinRegistry.js && node --check src/skins/SkinPacks.js && node --check src/systems/InputController.js && node --check sw.js && node --check src/Game.js && node --check src/main.js",
    "serve:test": "node scripts/serve-test.js"
  },
  "engines": { "node": ">=18" }
}
