# 安裝手冊

## 遊玩安裝

1. 將整個專案資料夾複製到 Windows 電腦。
2. 確認資料夾結構完整，不要只複製 `index.html`。
3. 安裝最新版 Chrome 或 Edge。
4. 雙擊 `index.html` 即可，不需網路與伺服器。

## 開發環境（選用）

安裝 Node.js 18 或更新版本後，在專案根目錄執行 `npm test`。本專案不需 `npm install`。
