> **v0.85.42：** 無玩家資料遷移；回復時同步回復 2-2 地圖 PNG、地圖／任務／壓力／怪物／HUD、HTML、測試與 Service Worker，切勿只回復單一腳本。見 [回復說明](CHAPTER2_MAP_2_2_V08542.md)。

> **v0.85.39：** 無玩家資料遷移；回復時同步回復荒風系統、地圖資料、怪物移動、HUD、HTML 與 Service Worker，切勿只回復單一腳本。見 [回復說明](MAP_PRESSURE_V08539.md)。

> **v0.85.38：** 無玩家資料遷移；回復至 v0.85.37 須同步還原倍速保護、提示 UI、樣式和 Service Worker 版本。見 [回復說明](LONG_SESSION_FOLLOWUP_V08538.md)。

> **v0.85.36：** 無存檔 schema 變更；變身與施法動畫是自動過期的戰鬥暫態資料。回復時同步回復呈現腳本、版本與 Service Worker。見 [回復說明](HERO_EVOLUTION_ANIMATION_V08536.md)。

> **v0.85.33：** 新存檔同時包含一般玩家第 1 輪與管理者檔；舊存檔維持原啟用身份。匯出完整玩家檔後再更新；回復程式至 v0.85.32 不需轉換存檔。見 [備份與回復](PLAYER_ENTRY_FRAME_PACING_V08533.md)。

> **v0.85.31：** 無玩家資料遷移；回復時把戰鬥腳本、頁面、樣式、版本與快取一同還原至 v0.85.30。見 [回復步驟](FRAME_PACING_V08531.md)。

> **v0.85.30：** 圖鑑肖像只新增靜態美術與讀取路徑，沒有存檔遷移。若需回復，將六張圖、三個圖鑑腳本、版本標示與 Service Worker 快取一併回到同一版；玩家資料保持不動。見 [圖鑑軍團肖像交付](CODEX_FACTION_ART_V08530.md)。
> **v0.85.32：** 章節快照新增可選欄位 `economy.emblems`、`hero.evolution`；舊快照缺欄位會以未進化／0 勳章安全還原。見 [英雄進化交付](HERO_EVOLUTION_V08532.md)。

> **v0.85.26：** 無存檔遷移；回復成本、版本檔與 SW 快取即可，玩家資料保留。見 [地精前期平衡](GOBLIN_EARLY_GAME_V08526.md)。

> **v0.85.25：** 無存檔遷移；回復時將兩張怪物圖、程式、版本頁與 SW 快取一起還原至 v0.85.24。見 [回復步驟](COMBAT_MOTION_V08525.md)。

> **v0.85.24：** 沒有存檔遷移；回復時連同地精程式、版本號與 SW 快取還原，玩家資料保留。見 [供能可讀性熱修](GOBLIN_NETWORK_VISIBILITY_V08524.md)。

> v0.85.20 怪物攻擊站姿與路徑銜接：參見 [完整操作、架構、部署、回復與驗收](COMBAT_MOTION_V08520.md)。

> **v0.85.19：** 沒有存檔遷移；教學步驟與當局掉落都不寫入玩家備份。回復時連同腳本與快取版號回到同一版。詳見 [備份與回復](CHAPTER1_ONBOARDING_V08519.md)。

> **v0.85.18：** 無存檔遷移；回復上一版時一併還原動作程式與快取版號。詳見 [備份與回復](COMBAT_MOTION_V08518.md)。

> **v0.85.13（2026-09-24）：** 美術更新不遷移存檔；先匯出玩家資料，回復時一起還原程式與快取版號。詳見 [軍團動作圖回復](FACTION_MOTION_V08513.md)。

> **v0.85.12（2026-09-24）：** 劇情卡不修改玩家存檔格式；更新前仍可由設定匯出備份，回復時需一起回復程式、CSS 與快取版號。詳見 [劇情卡回復](STORY_CARDS_V08512.md)。

> **v0.85.11（2026-09-23）：** 本次不改玩家資料格式；以 Git revert 回復修復提交，程式、CSS 與快取版號需一起回復。 詳見 [手機版修復說明](MOBILE_RANKING_INPUT_V08511.md)。

> **v0.85.10（2026-09-23）：** 更新不操作 localStorage 或 IndexedDB；程式回復使用 git revert 修復提交並同步回復 SW 與入口版本。 詳見 [更新修復說明](UPDATE_RECOVERY_V08510.md)。

> **v0.85.9：** 變更前 21 個檔案與 SHA-256 清單位於 `artifacts/deployment-visual-v0859-backup/`；逐檔回復並同步舊版快取／標示，保留其他未提交工作。詳見 [地圖部署視覺整理](DEPLOYMENT_VISUAL_V0859.md)。

> **v0.85.8：** 本輪變更前 22 個檔案保存於 artifacts/frostland-balance-v0858-backup；依 manifest 逐檔比對回復，保留其他未提交工作。見 [霜原平衡交付](FROSTLAND_BALANCE_V0858.md)。

# 備份與還原手冊

> **v0.85.7（2026-09-23）：** 地精調整前快照保存在 artifacts/goblin-polish-v0857-backup；依 manifest 比對回復，保留其他未提交工作。 操作、架構、部署、回復及驗收見 [地精視覺交付](GOBLIN_POLISH_V0857.md)。

> **v0.85.6：** 回復舊速度須一起還原 `TDGame.js`、版本顯示和 Service Worker 快取名稱；玩家存檔格式未變。見 [速度調整](GAME_SPEED_V0856.md)。

> **v0.85.5（2026-09-23）更新：** 桌機建造／技能介面放大；霜原七塔正式美術及四技能圖示／特效；排行榜僅收錄自由遠征完成至少 16 波的成績，原始戰報保留。當前操作、API、架構、安裝部署、回復與驗收以 [本版交付文件](BATTLE_POLISH_V0855.md) 為準。

v0.85.3：更新前匯出玩家備份並保留上一版完整靜態目錄；回退需一起還原圖片、地圖規則與 Service Worker。見 [戰場回復說明](STORY_MAP_REFINEMENT_V0853.md)。

v0.85.2：邊境對戰沒有獨立資料，無需遷移；回復時以同版完整靜態檔案和快取為單位。見 [移除與驗收紀錄](VERSUS_REMOVAL_V0852.md)。

v0.84.9 沒有改動玩家檔案或本局存檔格式。回復舊介面需一併回復 `ArmoryUI.js`、HTML/CSS、`ArmorySystem.js` 與 Service Worker；詳見 [軍械庫更新](ARMORY_V0849.md)。

v0.84.8 劇情關卡與地圖解鎖進度屬玩家檔案；更新或回復前的操作見 [三張劇情戰場](STORY_MAPS_V0848.md)。

> **v0.84.7 現行商城：** 四位英雄外觀（獸人為「冥骨帝王」）與「日蝕王庭」已開放使用玩家檔案的試用水晶購買、保存與裝備；沒有真實付款。詳細規格、操作、限制、部署與測試見 [外觀上架交付](COSMETIC_RELEASE_V0847.md)。

> v0.84.6：更新結算畫面前先由遊戲設定匯出玩家備份；存檔失敗時保留頁面並排查儲存空間，不要清除瀏覽器資料。局部程式回復參考見 [本版交付](RESULT_SCREEN_V0846.md)。

> v0.84.4：更新前從設定匯出完整玩家檔案，包含新 `profileArt` 與自由遠征戰報。回復舊程式時保留原始玩家資料，勿清除本機儲存。詳見 [本版回復](EXPEDITION_SCORE_PROFILE_V0844.md)。

> v0.84.3：修改前快照存於 artifacts/chief-shockwave-v0841-backup/；先備份現況再逐檔比較回復，勿覆蓋同期未提交內容。詳見 [技能交付](CHIEF_SHOCKWAVE_V0843.md)。

> v0.84.2：更新前先匯出玩家備份；排行榜只讀資料，資料損毀不自動覆寫，回復舊版本無須刪除 localStorage。詳見 [排行榜備份與回復](RANKING_UI_V0842.md)。

> v0.83.6：修改前快照為 artifacts/battle-v0836-backup/。若同檔案已有其他開發，僅撤回本次差異，勿整檔覆蓋；保留正式玩家資料。 詳見 [右鍵與戰鬥版型修正](BATTLE_INPUT_LAYOUT_V0836.md)。

> v0.83.5：先匯出玩家備份。整合前程式快照位於 artifacts/integration-v0835-backup/，依相對路徑覆回可回復舊主入口；保留 localStorage 與其他未提交工作，不要 hard reset。 詳見 [商城與圖鑑整合交付](FEATURE_INTEGRATION_V0835.md)。

> v0.83.4：修改前快照位於 artifacts/lobby-v0834-backup；先匯出玩家備份，再依同名相對路徑回復快照。不要清除 localStorage 或整體重設其他工作。見 [回復流程](LOBBY_LAYOUT_V0834.md)。

> v0.83.3：先從設定匯出完整玩家備份。觀看 ID 與 Codex envelope 隨原有備份保存。本次程式修改前快照在 artifacts/prologue-v0833-backup，依原相對路徑覆回可回復；保留其他尚未提交工作，不使用 git reset --hard。詳見 [序章回復流程](PROLOGUE_CINEMATIC_V1.md)。

> v0.83.2 不變更存檔。若要撤回節奏試調，先備份瀏覽器本機紀錄，再整套回復至 v0.83.1（含 `WaveCatalog.js`、`WaveSystem.js`、版號與快取）；不要只混用舊新波次腳本。

> v0.83.1 未更動存檔結構。還原前備份瀏覽器本機資料；要回到 0.83.0，須整套回復程式、`td.html`、`sw.js` 與 `package.json`，勿只回復數值模組；既有戰績可保留。

> v0.73.0 本機戰績新增可選分析欄位但沿用 V3 格式。還原舊版不需資料遷移；舊版會忽略未知欄位。

> v0.72.0 還原時同步回退 BattleReportSystem、BuildSystem 交易回呼、TDGame 接線、戰況 UI 與快取版本。既有本機積分格式相容。

> v0.71.4 還原需同步回退四難度 `reward`、EconomySystem倍率下限、UI、測試與快取版本；既有戰績不受影響。

> v0.71.3 還原時同步回退難度倍率、軍備鎖、WaveSystem 縮量規則、UI 與快取版本。玩家戰績不需清除。

## 0.71.0 暮秋遺跡與整合發布

新增第三張「暮秋遺跡 · 雙 U 型彎」，採縮窄第一個內圈的確認版本。遠征頁可直接選擇，沿用英雄／軍團／波次及地圖獨立戰績；路線、建造區、小地圖與离線資產同步，無新帳號、API 或設定。完整安裝、更新、架構圖、備份回復與測試見 [發布手冊](AUTUMN_RUINS_V0710.md)。

> v0.70.0 還原點包含士兵倍率 `[1,1.3,1.7,2.2]`、版本號及服務工作者快取。還原程式不需刪除本機戰績；舊局已部署士兵會依目前程式計算下一級費用。

## 0.69.9

還原本次平衡時，先備份當前專案，再以版本差異回退 [定位調整](ROSTER_BALANCE_V1.md) 列出的欄位與程式。工作區另有同步修改，不可用整體重設覆蓋。

## 0.69.8 地圖獨立積分

更新前備份 localStorage 的 heroFrontierScoreRecordsV1 原始字串。V2 與 V1 使用同一儲存鍵；載入只轉換記憶體，下一次結算才保存 V2。舊版本不理解 bestByMap，回退程式時應同時還原更新前儲存備份，避免只回退程式導致新分組遺失。artifacts/map-score-backup 保存修改前兩個程式檔及版本資訊；共享工作區有後續修改時以差異還原，勿直接覆蓋。

## 0.69.6 士兵裝備顯示修正

本次修改前的 ArtSystem.js、package.json、sw.js 快照保存於 artifacts/equipment-visual-backup，version.json 記錄前後版號。只在沒有後續修改時才可直接還原快照；多人同時修改時以差異比對還原裝備呈現區段，勿覆蓋其他功能，並更新快取名稱。

> 0.69.2 的檔案級回復清單與資料相容性見 [UNIT_ACTIONS_V0692.md](UNIT_ACTIONS_V0692.md)。

## 0.68.5 馭獸師與熊戰鬥動作（2026-09-17）

恢復馭獸師原本的鹿角、肩鳥與綠袍設計，新增待機及施法畫格；三階熊新增踏步與撲咬畫格，近戰命中顯示爪痕。使用既有 state、frameClock、attackTimer 切換，無資料庫、API 或權限變更。完整安裝、更新、回復與測試方式見 [動作更新紀錄](NATURE_MOTION_V0685.md)。


## 0.68.4 森靈馭獸師造型（2026-09-17）

修改前備份位於 artifacts/beastmaster-v0684-backup，按相對路徑還原即可回到原造型。原 faction-builds-v1.png 保留不變；不需清除戰績。


## 0.68.0 雙隘口要塞（2026-09-17）

修改前檔案位於 artifacts/twinpass-v0680-backup；含其他既有工作，請逐檔比较回復。可先隱藏第二張選項，保留第一張與雙路引擎。

詳見 [第二張地圖規格、操作與回復](TWIN_PASS_V0680.md)。

## 0.67.0 終極士兵（2026-09-17）

本功能修改前快照位於artifacts/ultimate-soldiers-backup。還原時保留其他同步工作，以差異合併處理；完整發行回退需使用整包版本及一致快取，勿只覆蓋package.json。詳細步驟與限制見專章。

詳見[終極士兵操作、架構與驗收](ULTIMATE_SOLDIERS_V1.md)。

## 0.66.5 直接開檔修復（2026-09-17）

本次修改前備份 artifacts/file-start-v0665-backup，ArtSystem.js 對應 src/td/systems/ArtSystem.js，其餘按原相對路徑。回復該備份將重新引入直接開檔問題；建議保留此修正版。


## 0.66.4 建造預覽對齊（2026-09-17）

本次修改前備份位於 artifacts/preview-v0664-backup；按相對路徑還原其中檔案即可復原本次修改前狀態，再重新整理網站快取。保留 localStorage 及其他工作內容。


## 0.66.2 選取用途說明（2026-09-17）

本次修改前檔案保存在 artifacts/selection-v0662-backup，包含原有未提交內容。回復時停止伺服器，按相對路徑覆回該備份內檔案，移除本版新增 tests/td-selection-purpose.test.js；重啟並更新瀏覽器快取。不要刪除 localStorage。


## 0.66.1 新手谷地美術（2026-09-17）

v1 原圖保留；局部回復需同步修改 maps.js、ArtSystem.js、sw.js 的素材路徑並更新快取名稱。不要整包覆蓋含其他未提交工作的本機備份。

詳見 [地圖更新、架構與回復手冊](MAP_ART_V0661.md)。

## 0.65.0 手機介面修正（2026-09-17）

本次修正前的 Git 基線是 `89227f9`（0.64.0）。可用 `git archive --format=zip --output=../hero-frontier-v064.zip 89227f9` 匯出完整舊版到另一個檔案，解壓至新目錄驗證；不要覆蓋尚未保存的修改。發布前備份整份目前工作區及原有 localStorage 戰績。回復時使用整版程式與更新的快取名稱，避免混用。手機容器不變更同源 localStorage 鍵，不新增儲存遷移。

## 0.64.0 備份／回退

改名不改內部 ID、localStorage key 或戰績格式。亡魂、召喚及搖桿都是單局狀態，不新增長期存檔；歷史戰績保留當時的名稱快照。

發布前備份完整專案與 `heroFrontierScoreRecordsV1` 網站資料。前一個正式基線為 `0037d5c`（0.62.0）；0.63.0 是本次開發中間號，未獨立發布。需回退時，使用 Git revert 回退本版提交並建立新的回退版本／Service Worker cache 名再正常推送；不要 force push 或只換回其中一個腳本。也可將舊提交另行 checkout 到新資料夾驗證，不覆寫正在工作的資料夾。

更新靜態檔案不會主動清除積分；清除網站資料前先匯出 localStorage。詳見下方既有戰績備份說明。

## 0.62.0 戰績備份

程式仍是靜態檔案，沒有伺服器資料庫。玩家積分紀錄位於瀏覽器網站資料的 `localStorage` 鍵 `heroFrontierScoreRecordsV1`，內容含版本、最高分與最近 20 場。一般程式更新不會清除；清除網站資料、換瀏覽器或換網域會失去紀錄。需要保留時，請先透過瀏覽器開發工具匯出該鍵，再於相同來源還原。格式損壞時系統會安全回到空紀錄，不影響開局。

## 0.61.0 備份與回復

沒有存檔 Schema 變更；積分、裝備與兌換餘額都仍是單局記憶體狀態。備份／回復需讓 `TDGame.js`、Camera、Economy／Shop、BattleReport、HTML／CSS、manifest、測試、文件、`package.json` 與 `sw.js` 保持同版。回復 0.60.1 會失去觸控確認、雙向兌換與積分 UI，回復後也必須同步切換 Service Worker 快取名稱。

## 0.60.1 備份與回復

沒有存檔 Schema 變更。備份與回復時需讓 `td.html`、`TDGame.js`、`main.js`、`package.json` 與 `sw.js` 同版；回復 0.60.0 會恢復錯誤跨遊戲連結，因此應優先保留 0.60.1 完整提交作為可回復基線。

## 0.60.0 備份與回復

本版沒有存檔 Schema 變更。備份與回復時讓 `td-combat.css`、`package.json`、`sw.js`、測試及文件保持同一版本即可；不要只回復 CSS 而保留 0.60.0 快取名稱。回到 0.59.0 後需同步切回對應 Service Worker，再重新載入頁面。

## 0.59.0 備份與回復

本版沒有存檔 Schema 變更。備份／回復必須讓 `assets/td/opening`、三個選角資料檔、`TDGame.js`、`td.css`、`package.json` 與 `sw.js` 保持同版；只回復圖片或只回復程式都可能造成離線快取缺圖。選角素材均為本機檔案，可在獨立備份中直接保存。

## 0.58.0 備份與回復

本版無持久化資料格式變更。備份需保留完整 Git 工作樹，特別是 0.57.0 正式地圖提交與目前 0.58.0 尚未提交修改；不得用 reset／discard 只留下 UI。回復時應讓 `Monster.js`、`EnemyCombatSystem.js`、`TDGame.js`、`maps.js`、Art／Terrain、HTML／CSS、`package.json` 與 `sw.js` 回到同一版本。英雄裝備與戰利品仍為單局記憶體資料，不需資料遷移。

## 0.57.0 備份與回復

本版沒有新增戰局存檔或資料庫。備份／還原必須以完整 Git 版本為單位，同步保留正式 PNG、`maps.js`、鏡頭／地景腳本、HTML／CSS、Service Worker 與文件；不能只替換地圖圖片，否則道路和建造多邊形會錯位。發布後可用 0.57.0 commit 回復；若回復舊版，也要同步回復 `CACHE_NAME`，再由瀏覽器更新 Service Worker。

## 0.55.0 士兵規則備份（2026-09-15，本機）

本遊戲仍未新增戰局存檔或資料庫；備份時保留整個專案工作目錄與未提交修改，尤其 `src/td`、`td.html`、`td.css`、`package.json`、`sw.js` 及文件。還原至上一版本應使用已知版本快照並同步還原 Service Worker 版號，不能只換單一腳本。裝備持有關係只存在本局記憶體，重新開局不延續，無資料轉換。

## 0.54.3 商城與技能分區（2026-09-14，本機）

本版沒有存檔或資料轉換。備份、還原及發布時讓 `td.html`、`td.css`、`td-combat.css`、`TDGame.js`、`package.json` 與 `sw.js` 保持同版；勿丟棄現有未提交修改。

## 0.54.2 士兵升級圖塊（2026-09-14，本機）

本版沒有存檔轉換；備份／還原時保持 `ArtSystem.js`、`package.json`、`sw.js` 與其他同版遊戲檔案一致。不要清除既有本機進度或丟棄未提交修改。

## 0.54.1 英雄小卡（2026-09-14，本機）

本版不變更存檔。若需還原舊英雄 HUD，使用同一版本備份中的 td.html、td.css、td-combat.css、package.json、sw.js 整組還原，避免 HTML／CSS 與離線快取版本不一致；勿丟棄其他未提交的遊戲修改。

## 0.54.0 小地圖與快捷面板（2026-09-14，本機）

還原到舊介面需使用先前完整備份的 td.html、td-combat.css、src/td/main.js、src/td/TDGame.js、package.json 與 sw.js，並移除僅此版引用的 MiniMapView.js；玩家存檔格式未變。勿直接丟棄目前含多輪成果的工作樹。

## 0.53.0 戰鬥 HUD（2026-09-14，本機）

回復此版外觀需保留先前完整備份的 td.html、td.css、td-combat.css 與相應 sw.js；不需回復玩家資料。勿丟棄目前工作樹中其他未提交成果。

## 0.52.0 地圖構圖與部署體驗（2026-09-14，本機）

本輪保留 frontier-ground-v1.png。只回復地表外觀可把 ArtSystem.frontierGround 指回 v1，並更新發布版本／快取；這不是整版回復。完整回復請使用先前完整備份，勿 git reset/discard 目前含多輪成果的未提交工作樹。未修改任何玩家存檔結構。

## 0.51.0 HUD／地景呈現（2026-09-14，本機）

需連同frontier-ground-v1.png、hero-skill-icons-v1.png與同版JS／CSS／HTML／sw一起備份。舊原圖素材未覆蓋，開局仍可切回classic地圖；切圖不等於回退UI版本。

## 0.50.0 大地圖原型（2026-09-14，本機）

新圖不用遷移存檔。測試比較可在開局直接選原版地圖；這只是選回原圖，不是回退程式。正式版本回復需保存整套同版HTML／CSS／JS／sw，勿只替換maps.js。現有未提交成果應整體備份，禁止reset丟棄。


## 最新：0.49.0 鏡頭原型

0.49.0備份新增Camera腳本與tests/td-camera.test.js，連同TDGame、main、HTML、CSS、sw一起保存。沒有建立本輪commit；不能把版本號當作已存在的回復點。切勿回退本工作目錄所有既有未提交修改。

## 0.48.0 邊緣 HUD

備份需包含新增 `td-combat.css`、`tests/td-edge-ui.test.js` 以及原 HTML／main.js／sw.js。尚未建立此次更新的 Git commit，版本號不等於已有可切換的提交。回復應採已保存的整套配對版本，不可 reset 目前含其他未提交成果的工作目錄。

## v0.47.0 本機回復注意

此次發布前先保留目前整個專案及既有未提交的 v0.45–0.46 修改；不能用 `git reset --hard` 回退，否則會一起失去先前成果。要單獨回退此版，需比對 v0.47.0 變更檔（見進度紀錄）並還原相關 UI／放置流程，或在日後正式 commit 後以該 commit 進行受控回退。無資料庫或儲存格式遷移；介面偏好仍在瀏覽器 localStorage。PWA 更新可透過重新載入啟用 `sky-strike-v0.47.0` 快取。

## v0.46.0 備份與回退

本機尚未提交或發布，請先備份完整工作目錄與未提交檔案。回退需在**另一份資料夾／工作樹**取回 v0.45.0 或最近已發布版本，先確認英雄 × 軍團改動是否也需要保留，不要對現用工作樹強制重設。回退本版會移除戰鼓支援、弩砲支援怪優先及兩種召喚塔的新節奏；沒有存檔格式、帳號或資料庫遷移。若日後上線並回版，也需提升 Service Worker 快取名稱並重開遊戲分頁。

## v0.45.0 備份與回退

目前版本尚未推送 GitHub；備份整個工作目錄（包含未提交變更）與已發布的 v0.44.2 提交。若要回到上一版，在**另一個資料夾／工作樹**取回已發布版本，先比對 `td.html`、`TDGame.js`、`sw.js` 與文件，勿對現用工作樹強制重設。此版沒有永久存檔格式、資料庫或帳號遷移；回退會恢復英雄與軍團同 ID 綁定及舊塔技能行為。若未來已部署 Pages，需更新快取版本並在裝置上關閉舊分頁重開。

## v0.44.2 備份與回退

本版只改怪物繪製、視覺回饋、測試與文件，沒有存檔格式或資料庫變更。備份整個專案及未提交檔案；需要回退時在獨立資料夾取 v0.44.1，勿在現用工作樹強制重設。回退會恢復單列怪群與普通怪常駐血條；若已部署，關閉舊分頁重開以更新離線快取。

## v0.44.1 備份與回退

本次只有 `ArtSystem.js` 武器圖層與測試／文件／離線快取版本變更，沒有存檔格式或資料庫變更。回退時在獨立資料夾檢出已發布的 v0.44.0 提交 `5ea2301`，不要對現有工作樹執行強制重設；舊版會恢復巡林者弓斜穿胸前的外觀。發布換版後關閉所有遊戲分頁再開，讓 Service Worker 更新。

## v0.44.0 備份與回退

本版只改本機靜態 UI／文件及 Service Worker 快取版本，沒有存檔、帳號或資料庫 schema 變更。備份整個專案與未提交檔案；若需回退，於獨立資料夾檢出 v0.43.0 對應提交，不要在現用資料夾執行破壞性重設。回退後 PC 會恢復右側長建造清單，選角會立即進場，遊戲選單／新結算按鈕消失；本機遊戲資料不需清除。若已發布換版，關閉全部遊戲分頁再開以刷新離線快取。

## v0.43.0 備份與回退

本次無存檔／資料庫 schema 改動。更新或回退前保留整份專案及未提交工作樹，不在目前資料夾執行破壞性重設；若需舊版，在獨立目錄取先前標籤。回退 v0.43.0 會回到可能停在美術 0/3 的載圖方式，並失去士兵新索敵。Service Worker 換版後關閉所有遊戲分頁再開，避免新舊腳本混用。本機玩家資料不需清除。

## v0.42.0 備份與回退

此次只有靜態程式、介面與離線快取名稱，沒有存檔或資料庫遷移。回退前先備份未提交檔案與本機遊戲資料；若需回到上一版，於**獨立資料夾**檢出本機 `v0.41.0` 標籤，不要直接清除目前工作樹。回退會恢復較便宜的升塔、舊老兵曲線，並失去王國寒鋼哨塔。更新或回退後關閉遊戲分頁再重開，使 Service Worker 不混用版本。

## v0.41.0 還原

本版無存檔或資料庫遷移。回退前備份未提交檔案，可在獨立目錄檢出上一標籤 `v0.40.1`；回退會失去 F 大絕與手機素材載入保護。瀏覽器關閉舊分頁再重開，讓 Service Worker 換版；不要手動刪除玩家本機資料。

## v0.40.1 還原

本版僅新增一張圖片並更改本機引用與快取，不改存檔。回退可在獨立資料夾檢出 `v0.40.0`，但會恢復購買王國武器後的棋盤格問題；請先備份目前工作樹與未提交檔案。回退或更新後須關閉舊分頁重新開啟，避免舊 Service Worker 混用。

## v0.40.0 還原

本次只擴充本機程式與 PNG，沒有改動永久存檔欄位。建議回退前先另存目前工作樹及瀏覽器本機資料；若需回到上一版，使用本機 `v0.39.0` 標籤在獨立資料夾檢出，或保留本版後再複製上一版完整檔案，不要只還原程式而留下新舊混用的 `sw.js`。回退後關閉遊戲頁並重新開啟，必要時移除該網站離線快取。Git 標籤不會自動備份未提交的個人檔案。

## v0.39.0 資料相容性

本版只新增圖集、顯示路由、戰鬥設定與一個本機 UI 偏好 `towerFrontierMobilePanel`，不改戰役存檔。由於 v0.38.0 的調整一併收錄在本次本機提交，完整回退可使用 `artifacts/before-v0.38.0.zip`，或回到既有標籤 `v0.36.0`；刪除該偏好鍵即可重設手機面板。回退後需關閉頁面再重開，避免 Service Worker 混用素材。

## v0.38.0（2026-09-12）

本次修改前工作檔已備份於 artifacts/before-v0.38.0.zip，包含當時未提交的程式與文件。要撤銷本次變更：先另外備份目前專案，再把 ZIP 解壓至空目錄，核對後將其中同名檔案覆蓋回專案；保留原 assets（本次未改圖片）。回退後重啟遊戲，若使用 PWA 請移除該網站快取後重新載入。此 ZIP 是本次修改回退包，不是獨立完整發行包。

## v0.36.0 資料相容性

本版只新增設定、程式與本機 PNG，沒有新增永久存檔欄位。回退 v0.35.0 會移除騎士、樹靈、魔像並恢復術士／祭司／Boss 舊外觀；進行中的單局會重開，本機難度、介面及射擊模式紀錄不受影響。可用本機標籤 `v0.35.0` 回復上一版。

## v0.35.0 資料相容性

英雄與塔更新都是程式及本機素材，沒有新增存檔欄位。還原到 v0.34.0 會恢復共用英雄外觀、每族 3 座開局塔及 10 種建築；進行中的單局仍會重新開始，瀏覽器偏好不受影響。

## v0.34.0 資料相容性

30 波與 Lv.15 都是單局記憶體狀態，沒有新增永久儲存欄位或資料遷移。回退到 v0.33.0 會恢復 15 波與 Lv.10 上限，進行中的戰役會重新開始；外觀與介面偏好不受影響。

## v0.33.0 資料相容性

此為 v0.33.0 歷史狀態。0.55.0 沒有士兵負傷資料；若回退到舊版，舊士兵承傷／離場規則會恢復。軍械、等級與熟練仍只屬於目前這一局，重新整理會重開戰役。

> v0.30.0 沒有新增持久化存檔格式，還原舊版不需要資料轉換。單局英雄 XP、戰利品與軍團解鎖不寫入 `localStorage`；頁面重新載入即安全重置。

## 備份

- 版本備份：保留 Git 儲存庫並建立版本標籤。
- 可攜備份：將整個專案資料夾壓縮為 ZIP。
- 沒有玩家帳號、伺服器進度或資料庫需要另外備份。外觀、難度、最高分與最高 Combo 只存在瀏覽器 `localStorage`；清除網站資料後會回到預設值。

## 還原

使用 Git 時可從版本標籤建立新分支或工作目錄：`v0.1.0` 為最小可玩版、`v0.8.0` 為百波遠征、`v0.10.0` 為英雄塔防首發、`v0.13.0` 為可移動軍團版、`v0.14.0` 為 A* 戰術指令版、`v0.15.0` 為跨裝置介面版、`v0.16.0` 為塔防波次循環版。不建議直接覆寫尚未提交的檔案。

備份 v0.16 時必須包含完整 `assets/td`、三張動作圖集，以及 `WaveCatalog.js`／`EconomySystem.js`／`WaveSystem.js`／`LayoutSystem.js`／`NavigationSystem.js`／`CommandSystem.js`；所有 PNG 與程式碼一同由 Git 管理，不依賴圖片生成服務在線上執行。

塔防的波次、金幣、塔與城門均為單局記憶體狀態，重新整理或關閉頁面後歸零，不需另外備份。未來加入存檔時必須同步擴充此文件與資料遷移流程。

## v0.17.0 英雄與指揮介面

v0.17.0 新增英雄圖集、互動與指揮面板；備份須包含 assets/td/hero-actions-v1.png。上一個基線為 v0.16.0，回復時在另一工作目錄檢出該標籤，避免覆蓋未提交內容。仍不保存塔防局內進度。

## v0.18.0 英雄技能與種族塔

完整備份專案與 Git 歷史，包含新增圖集。回退基線 v0.17.0，建議在另一目錄檢出該標籤，保留目前工作。局內塔熟練度、英雄裝備、召喚物未做存檔，重載或回退會消失；介面偏好仍沿用既有儲存。

## v0.19.0 英雄流派與傭兵館

v0.19.0回復基線為v0.18.0。完整備份包含新增 HeroRoster.js；需要回復時在另一目錄檢出v0.18.0保護現有工作。仍無局內存檔，裝備、傭兵、地面技能重載皆重置。

## v0.20.0 敵軍動作第一階段

完整備份包含三張新增敵軍圖集。回退基線v0.19.0，建議在另一目錄檢出保留目前工作。局內進度仍未存檔。

## v0.21.0 RTS 指揮介面

完整備份需包含 HTML、CSS、TDGame、main、測試、文件與 `sw.js`。回退基線為 `v0.20.0`；請在另一工作目錄檢出以避免覆蓋現有內容。速度、暫停與局內狀態均不保存，介面模式偏好仍由既有 `localStorage` 管理。

## v0.22.0 戰鬥回饋

完整備份須包含新增回饋模組及相關實體修改。可回退到 `v0.21.0` 取得只有新版 HUD、沒有新戰鬥回饋的穩定版本；回退不影響任何存檔格式。

## v0.23.0 戰鬥生命週期

完整備份須包含 `EnemyCombatSystem.js`、實體、TDGame、HTML、Service Worker、測試與文件。回復舊版時必須同步回復 `td.html` 與 `sw.js` 的腳本／快取清單；使用 Git 標籤 `v0.23.0` 可回到完整一致版本。

## v0.24.0 黃金 15 波

完整備份新增 `TDDifficultySystem.js`、`EnemyTraitSystem.js` 與新版波表。回復時不可只還原 `td.html`，必須同步還原 Service Worker 快取名稱與全部相依程式。完成封存後可使用 Git 標籤 `v0.24.0` 回到此版本；局內波次仍不保存。

## v0.31.0 戰地軍械

備份需同步包含 `ArmorySystem.js`、軍械圖集、LootSystem、Hero、CombatUnit、ArtSystem、TDGame、HTML、CSS 及 `sw.js`。回復 v0.30 時必須整組回復，避免 HTML 載入不存在模組或舊快取遺漏圖片。軍械只存在記憶體，沒有需要備份的玩家資料。

## v0.32.0 唯一軍械與守軍編成

備份需包含 ArmorySystem、BuildSystem、TDGame、HTML、CSS、測試與文件；軍械圖集沿用 v0.31，沒有新增二進位資產。建議以 v0.31.0 作回退基線並在另一目錄檢出。回退會恢復同件軍械可重複配置的舊規則，局內裝備狀態不會保留。

## v0.32.1 手機圖示

備份需包含圖示原圖、四種輸出尺寸、`build-app-icons.ps1`、兩份 manifest、HTML 與 Service Worker。回退至 v0.32.0 不影響玩法，但手機重新安裝時會恢復舊 SVG 圖示，從 TD 安裝也會開啟共用起始頁。

v0.32.2 只增加 Pages 首次啟用設定；回退至 v0.32.1 仍可本機遊玩，但全新 GitHub 倉庫需手動啟用 Pages。


## v0.66.0 戰旗光環

更新前複製完整專案資料夾為備份；回復時關閉遊戲，以備份還原整套靜態檔案（含 sw.js），清除本站 service worker／快取後重開。不要混用不同版本 src 和樣式。本次保留原有未提交修改，不自動覆寫或提交其他工作。


## 0.66.2 角色比例（2026-09-17）

修改前原始檔保存於 artifacts/scale-v0662-backup/。要還原比例，把其中 ArtSystem.js 複製至 src/td/systems/ArtSystem.js；package.json 與 sw.js 也有備份。還原後使用新的快取名稱並重新載入，避免舊快取混用。此次備份保留了工作區既有未提交修改。


## v0.66.3 侵蝕特效

本次修改前檔案已存 artifacts/corrosion-v0663-backup，包含原 v0.66.2 的工作中內容。關閉遊戲後依相同相對路徑複製備份檔回專案，再刷新／清除本站快取，即可回復本次核心程式修改；新增的 td-corrosion-vfx.test.js 與 qa-corrosion-v0663.cjs 亦應移出舊版本。若回復完整交付包，請使用更新前整份資料夾備份。

## 0.66.4 波次提示

波次預告改為怪物縮圖、數量與威脅標籤，新增入口倒數與短暫開戰提醒。操作、設定、架構、API、部署、還原與驗證方式見 [波次提示文件](WAVE_HUD_V0664.md)。



## 0.67.1 手機波次版面修正

修正波次與倒數重疊、速度列出界，縮小手機提示面板；無操作、設定或資料格式變更。原因、架構、更新、還原及觸控版面驗證見 [版面修正文件](WAVE_LAYOUT_V0671.md)。


## 0.68.1 波次避讓與預覽修正

波次改為靠左小列，預告按需展開，戰鬥提示可點穿；修正三種終極士兵卡片裁切。操作、API、架構、部署、還原與測試見 [版本文件](EDGE_WAVE_PREVIEW_V0681.md)。



## v0.68.2 藤蔓纏繞

本次修改前檔案已備份至 artifacts/vines-v0682-backup，保留既有未提交內容。關閉遊戲，依相同相對路徑複製回原位置可回復本次修改；若回復舊版測試集合，同時移出新增 tests/td-vines.test.js。清除本站快取後重開。

## 0.68.3 士兵尺寸微調（2026-09-17）

我方部署士兵統一放大 12%，保留相對體型與腳底錨點；英雄、怪物、召喚物和卡片大小維持原設定。無新增操作、設定、API、權限或資料格式。ArtSystem.drawCombatUnit → 腳底縮放 → drawCombatUnitSprite；預覽直接使用未放大的 sprite 方法，避免再次裁切。

安裝與部署流程不變，完整更新後關閉舊分頁再開啟。package.json 與 Service Worker 快取更新至 0.68.3。修改前 ArtSystem.js、package.json、sw.js 保存在 artifacts/soldier-v0683-backup/；若需還原，將 ArtSystem.js 放回 src/td/systems/，其餘放回根目錄，已有後續修改時先比較合併，部署使用新的快取名稱。

驗證：342 項測試通過；全角色圖在 artifacts/qa-unit-scale/roster.png；三種終極士兵的六張卡片預覽仍完整。此版本只改畫面尺寸，射程、攻擊、碰撞與移動數值不變。


## v0.69.0 英雄技能與動畫完整顯示

本次修改前檔案在 artifacts/hero-v0690-backup。期間若有其他工作，請先備份再比對還原，勿整包覆寫別的改動。還原舊入口、ArtSystem、英雄相關程式與 sw.js 後，新增模組可保留但不再載入。清除本站快取再重開；完整版本建議依更新前整份資料夾還原。
## 0.69.1 還原補充

恢復舊三選一流程的檔案與步驟見 [版本文件](FIELD_LOOT_SHOP_V0691.md)。

## 0.69.7 雙隘口路線校準

依正式圖片的鋪石路面重新取樣上下道路中心，修正彎道偏向路緣；共用末段長度同步計算。沿用 maps → Monster／BuildSystem／MiniMapView 架構，不改圖片、素材腳底或 API。重新開始遠征即可使用新座標，無設定與存檔遷移；部署完整版本與新的 Service Worker 快取。回復前資料在 artifacts/route-v0697-backup，請逐檔比較，避免覆蓋其他工作。

6 項雙路測試與語法檢查通過；新增獨立路面取樣回歸、完整路線怪物截圖，見 artifacts/qa-twinpass/full-route-battle.png。全套當次為 375/377 通過，兩項旗幟／統領光環測試失敗，涉及另行修改中的軍團平衡，未在此地圖修正中改動。
## 0.71.1 還原補充

程式回退不會刪除 V3 本機戰績；舊程式可能忽略 `bestByMapDifficulty`。若需完整還原資料格式，應先備份 localStorage 的 `heroFrontierScoreRecordsV1`，不要直接清除瀏覽器資料。
# v0.74.0 備份與還原

第三階段沿用 `heroFrontierScoreRecordsV1`，未改資料格式。清除網站資料會失去樣本；程式回退不需轉換紀錄，舊版會忽略新判定畫面。
# v0.75.0 備份與還原

無資料格式變更。既有戰局重新載入後採新版補給與升級規則；回退 v0.74.0 即恢復舊規則，不需清除本機戰績。
# v0.76.0 備份與還原

儲存鍵仍為 `heroFrontierScoreRecordsV1`，舊資料不刪除。新版讀取後會保留舊最佳分，但舊紀錄因缺少相同 `balanceVersion` 而不計入新平衡樣本；無需人工遷移。
# v0.77.0 備份與還原

章節進度儲存在 `heroFrontierChapterCheckpointV1`。備份瀏覽器站點資料可保留它；若資料毀損，系統會忽略存檔並允許正常開始新局。
# v0.79.0 回復

介面更新未變更任何持久資料格式；回復至 v0.78.1 不需轉換存檔。

# v0.79.2 回復

無存檔遷移。回復舊版只需還原 `td.html`、`td.css` 與 `src/td/main.js`；舊戰績中的策略標籤會繼續保留。

v0.80.0 沒有資料庫遷移。章節存檔的 faction 字串可保存 `wild`；回復 0.79.x 前應先完成或刪除荒野章節存檔。舊存檔若含 `orc-contract` 歷史仍能讀取，退役項目只是不再發放。


## 0.83.0 · 大廳與獨立測試輪次

設定與測試提供 JSON 匯出／匯入；匯入前另存 .beforeImport。新測試輪次會封存舊輪。回退程式的前版檔案在 artifacts/p0-v0827-backup。 完整操作、限制、架構與回復步驟見 [P0 說明](LOBBY_P0_V083.md)。

回復 0.82.7 時亦將 artifacts/p0-v0827-backup/BattleReportSystem.js 複製回 src/td/systems/BattleReportSystem.js。此備份保留本次改版前既有修改。

## v0.83.3

完整 JSON 備份包含各玩家與封存的 wallet；舊備份匯入會補零，更新前先匯出。介面回退不可覆蓋其他平衡修改。詳見 [備份與回退](LOBBY_EXPEDITION_V0833.md)。

# 地精 V1 檢查點

沿用 ProfileStore 存檔版本與既有章節檢查點 schema；新 `goblinNetwork` 欄位為可選，舊檔讀取時初始化為零。備份／還原原始 JSON 時勿刪除 `goblinNetwork`、裝置的 `recycleTimer` 或 `engineerBoost`。見 [地精 V1](GOBLIN_FACTION_V1.md)。

v0.84.5 沿用 `equipment.spear` 武器等級，無須存檔遷移；回復內容見 [武器紀錄](FACTION_HERO_WEAPONS_V0845.md)。


## 商城單一入口與雷霆戰王（0.6.1）

正式商城統一由 `td.html` 大廳或 `td.html?panel=shop` 進入；獨立 `shop.html` 與專用啟動程式已刪除，舊 4187 展示服務已停止。雷霆戰王沿用原有玩家檔案保存購買與裝備，四組動作已接入正式戰場。共用商城元件及既有收藏保留。

操作、API、部署、測試與回復限制見 [0.6.1 整合說明](shop/THUNDER_TD_V061.md)。

> v0.85.4 沒有玩家資料遷移；回復時採完整靜態檔案或 `git revert`，不可在有未提交修改的目錄硬重設。詳見 [命中回饋交付](HIT_FEEDBACK_V0854.md#安裝部署更新與回復)。
> v0.85.1：根目錄整合前的重疊檔案保存在 `artifacts/frostland-root-integration-backup-v0851/`；完整回復方式見[選角插畫交付](FROSTLAND_SELECTION_ART_V0851.md)。

v0.85.0：先匯出完整玩家備份。舊 schema 保持相容；冰原檢查點保存新兵塔與 equipment.spear，不保存瞬時寒冬、Frost、Hunt 與動作。降版時使用更新前備份，舊程式不認識冰原兵塔。回復流程見 [冰原存檔](FROSTLAND_FACTION_V1.md#j-save-migration--備份與回復)。
> **v0.85.14：** 新手引導完成旗標隨既有 story profile 備份；回復程式時請同步回復版本與 Service Worker。詳見 [第一章引導](CHAPTER1_ONBOARDING_V08514.md)。
> **v0.85.15：** 新版進度鍵為 `chapter1-border-onboarding-v2`，仍隨既有 story profile 備份；不影響角色與裝備。詳見 [引導修正](CHAPTER1_ONBOARDING_V08515.md)。
> **v0.85.16：** 吸附與容錯沒有新增備份資料；既有 story profile 可直接回復。詳見 [教學修正](CHAPTER1_ONBOARDING_V08516.md)。

> **v0.85.17：** 新兵補給和商城操作只存在當局記憶體，沒有新增備份資料；既有 story profile 可直接回復。詳見 [教學操作修正](CHAPTER1_ONBOARDING_V08517.md)。
> **v0.85.21：** 無存檔遷移；回復時連同新手教學腳本、樣式與快取版號回到同一版。詳見 [備份與回復](CHAPTER1_ONBOARDING_V08521.md)。
