# Combat Experience V2 進度（截至 2026-09-15）

## 2026-09-15：0.58.0 戰場清晰度與操作面板精簡

依實際遊玩回報補上同路怪物隊列間距、新手地圖英雄無傷、緊湊軍械／戰利品、桌面完整且由便宜到昂貴的建造 Grid、單一建造收合入口、戰鼓堡 Lv3 疊圖修正與正式地圖近看品質保護。全部沿用既有 Monster、EnemyCombat、Build、Armory、Loot、Art 與 Camera seam，沒有建立物理、第二套 UI 或新 Gameplay Map。選擇遠征的資訊層級草稿保留；使用者概念圖只作視覺基準，獨立立繪 Asset 尚未落地。本輪不是 P5。

## 2026-09-15：Battlefield & Camera V1＋永久固定士兵

0.57.0 將使用者提供的 1536×1024 圖片直接設為正式 World，校準單一路線、7 個草地建造區、安全區與鏡頭；PC／Mobile 共用幾何且不拉伸。敵軍取消 Visual Lane／Stagger，逐隻沿土路進場。士兵由「準備期可重新部署」更新為部署後永久固定；英雄移動保留；舊 M／A／H／S UI 移除，升級／回收移至單位旁。加入輕量火光與水面微光，但沒有建立地形引擎。完整紀錄見 `docs/BATTLEFIELD_CAMERA_V1.md`。這是已確認需求的延伸，不代表自行進入 Combat Experience V2 P5。

## 2026-09-15：士兵核心規則對齊（獨立於 V2 的後續需求）

0.55.0 本機版把士兵改為定點自動攻擊、準備期重新部署、不承傷／不擋路／不復活；每名士兵最多一件實體裝備副本。此段為歷史進度，0.57.0 已進一步改為永久固定。原 V2 的 P2／P3／P4 與 Battlefield／Camera／Canvas Audit 不重做；後續以 Repository、`CHANGELOG.md` 與 `docs/TEST_CHECKLIST.md` 為準。

## 最新：0.54.3 商城與技能分區

- 商店從 Q／W／E／F 技能列移至右側快捷列，保留唯一 `td-shop-open`、R 快捷鍵與既有商店流程；手機直向保留戰場右上角商店入口。
- 大絕名稱不再與冷卻秒數合併；名稱完整顯示，冷卻秒數由 `.skill-cooldown` 小角標呈現。僅 UI/UX 變更，無戰鬥數值或商品邏輯改動。
- 版本 0.54.3，未推送 GitHub；已做 PC／手機橫向／手機直向視覺驗收及商店開啟操作。後續接續以 Repository 實況為準。
- 驗證：237／237 自動測試、`npm run check`、`git diff --check` 通過；PC、844×390、390×844 截圖核對，大絕名稱完整，點擊與 R 鍵開店成功。Chrome 擴充套件記錄非同步訊息通道錯誤，沒有觀察到遊戲程式例外。

## 最新：0.54.2 士兵升級圖塊修正

- 依遊玩截圖移除 `ArtSystem` 守軍 Lv.2～5 的左右三角形、胸前幾何盔甲與頭頂菱形；改用素材輪廓光＋腳邊標記。召喚物共用標記已同步改善，建築標記保留。
- 只改視覺呈現與測試，不改戰鬥、升級數值、地圖或 UI。未推送 GitHub；後續以 Repository 實況為準。
- 驗證：236／236 自動測試、`npm run check` 與 `git diff --check` 通過；瀏覽器分頁操作中斷，實際遊戲內 Lv.2～5 的截圖驗收尚未完成。

## 最新：0.54.1 英雄戰鬥小卡精簡

- 完成 PC／手機橫向英雄卡常駐資訊精簡；XP、能力、武器移入「詳細資訊」，手機直向也可展開完整資訊。
- 保留原英雄選取、F1、TDGame 更新 ID、數值與戰鬥邏輯；修正無圖示武器名稱的格線排版。
- 本機版本 0.54.1，未推送 GitHub；接續開發以 Repository 為準，勿重做已通過項目。
- 驗證：235／235 自動測試、`npm run check`、`git diff --check` 通過；Chrome 1366×768、845×390、390×845 畫面及展開操作可用，Console 無錯；真機觸控仍待測。

## 最新：0.54.0 戰場小地圖與右側快捷圖塊

- 新增純呈現 MiniMapView：使用既有地表快取、道路、敵我單位及 BattlefieldCamera 畫縮圖和視野框；點縮圖呼叫原 camera.focus，不改戰鬥／部署座標。
- 選取圖塊控制原 `.selection-details`；軍械圖塊呼叫原 `TDGame.openArmory`。橫向手機的展開面板高度避開技能列；直向保留原 UI。
- 本機版本 0.54.0，未推送 GitHub；後續若接續 Combat Experience V2，以此進度及 Repository 為準，勿重做既有功能。
- 驗證：234／234 自動測試及 npm run check 通過；本機瀏覽器大圖／原版圖均可開啟並點縮圖，選取圖塊與軍械庫可用，Console 無錯；1920×1080、1600×900、1366×767、845×390 橫向與 390×845 直向無水平溢出。真機觸控、長局負載尚待驗。

## 最新：0.53.0 戰鬥資源列精簡

- 依使用者提供的單行概念圖，金幣／木材／功勳／城門改圖示數值，與原設定、暫停鍵視覺合併；不複製 ID 或事件，手機直向維持原版。PC 寬 336×38，窄高／手機橫向寬 310×34。
- 本輪未更動 Combat Experience V2 的戰鬥／鏡頭／地圖邏輯。GitHub 尚未更新。
- 驗證：231／231 自動測試、npm run check、git diff --check 通過；瀏覽器 1600×900、1366×767、845×390（含手機版橫向）、390×845 直向尺寸檢查通過；設定／暫停操作正常，Console 無錯。真機觸控及長局極大資源數值仍待實機確認。

## 最新：0.52.0 地圖構圖與部署體驗 V2

- 使用者批准接續地圖構圖／部署／進場取景，未授權本輪推送。開始227項測試通過，保留原dirty tree。
- 完成：地表v2分區材質、沿原Path的紋理路、建造時道路禁建與邊界提示、合法三區位置提示、開場取景。修正原西側提示位於路內。沿用BuildSystem判定抽出原因，不改合法面積。
- 已驗：230／230、語法check、diffcheck；瀏覽器1920／1366／845橫向／390直向，合法建塔與無效不扣款／取消不扣款，Console無error。詳細修改、圖片提示詞、測試及覆蓋取樣見 MAP_DEPLOYMENT_V052.md。
- 地圖路線／數值／AI／單位Scale不變；中央折返覆蓋長度偏高，需實戰驗證，未擅自平衡。真手機觸控、Edge、完整30波及長局FPS未驗，IAB截圖裁切仍在。
- **未完成最終地標美術**：沒有橋／城堡／新森林障礙，不要說成全概念圖完成。固定士兵與裝備新規則、P5未做。
- 本機0.52.0，沒有commit／push。下次先核對實際程式與測試，不重做已完成項目；大型地標或平衡須人工決定後接續。

## 最新：0.51.0 薄型 HUD／地景呈現 V1

- 使用者批准改善概念圖接近度後，先做UI減遮擋＋材質與技能圖像，不進入士兵規則／裝備／戰鬥改造。
- 修改：td.html、td-combat.css、main.js（Esc收合）、TDGame.updateUi（英雄圖示資料屬性）、ArtSystem（2張可選圖片）、FrontierTerrain（材質／沿路碎石／快取失效）、package、sw、兩項測試檔。新圖及提示詞記於ART_PRESENTATION_V051.md；其餘操作維護文件同步。
- PC頂部波次54px、资源與控制48px；小視窗改兩排。鏡頭預設只顯示折疊入口，全部原控制仍在。HP/MP並列、建造分類及關閉同行；845×390 Drawer由207縮到171px。
- 原創技能4×4圖集，三英雄不同列且保留文字fallback；Q/W/E/F效果與冷卻不改。R仍為商店，未因新圖改成新技能。
- 地面新增完整原創材質，原畫面大黑標籤改輕標示；道路沿既有Path，碎石固定seed且離屏快取，不改道路禁建與座標。圖片尚未載好沿用舊素材，兩圖都不新增開局阻塞。
- 227／227、自動語法check與diff check通過。測試補折疊UI保留入口、各英雄圖示映射、檔案／離線資源，以及新地面晚載入後快取更新。此前各階段測試仍保留。
- 瀏覽器驗證1920×1080、1366×767、845×390、390×845無水平溢出；頂列不互疊，鏡頭／情報可展開；建塔240→140G、5→3木；商店、召喚冷卻、三英雄圖示切換正常。Console未見error。
- 限制：內嵌瀏覽器截圖裁切仍在，不能宣稱完整全幅像素QA；真機觸控、長局FPS、30波平衡未驗。地景只改善材質，不是包含橋梁／城堡／複雜森林構圖的最終美術。
- 本機0.51.0，未commit／未push，不回退現有未提交成果。下一步先人工驗收UI和場景比例；需要大型地標時另對齊可部署區／導航，勿擅自新增P5或兵種裝備系統。

## 最新：0.50.0 銀葉隘口可玩路線原型

- 使用者批准「好 處理」後，接續0.49鏡頭，不重做已完成P2/P3/P4，不進入裝備或兵種改造。
- 新增 maps.js、FrontierTerrain.js、tests/td-map.test.js。修改TDGame、BuildSystem、Monster.progress、BattlefieldCamera、main、td.html、td-combat.css、package、sw及操作維護文件。
- 開局兩圖可切換：原版720×720保留；原型1280×900預設展示。地景只重用原美術草地／道路紋理，以離屏Canvas快取，尚不是概念圖等級的森林、橋梁、城堡場景。
- 路線共用給Monster／禁建／地景；西側彎道、中央折返與城門直線均為同一條Path，不是假造分岔。新圖道路中心55單位內禁止塔與士兵初始部署，原圖規則保留。其餘空間使用原間距／邊界限制。
- 原、新路線長度同為2017.831375，未改怪物速度／數量／HP／經濟。新圖折返索敵進度改為路段完成比例，避免向左走X減少導致排序倒退；原圖進度公式保留。
- 地圖切換只允許開局，共用原TDGame／reset，重建原PathSystem／NavigationSystem，更新英雄出生點與鏡頭邊界；retrySameSetup保存地圖id。沒有第二套Gameplay。
- PC／橫向用既有可拖曳鏡頭；直式以同一鏡頭等比例全圖顯示，避免1280×900被拉扁。未新增真機手勢或全面重做手機UI。
- 驗證：225／225、自動語法check與diff check通過。新7項測試涵蓋所有路段禁建、新區域合法、四種怪物×1／2／3無干擾抵達時間、折返單調進度、切圖／重開、導航／座標與地景快取。
- 瀏覽器：原邊界外(約1100,640)建塔成功，240→140G、5→3木；道路點拒絕且不扣款；放大／拖曳／回英雄、開波、×3、重開資源與第1波重置正常。Console無error。1920×1080、1366×767、845×390與390×845無水平溢出；手機模式經設定選擇測試。
- 限制：沒有完整30波平衡比較、真機觸控／Edge／長局FPS；原型火力覆蓋已改變，所以同路長不代表同難度。截圖仍有IAB80%裁切問題。士兵追擊／坦怪／裝備原邏輯保留，固定部署正式規則尚未做。
- 本機0.50.0，未commit／未推送GitHub，保留既有dirty tree。
- 下一步應先讓使用者驗收大地圖與镜頭手感，再決定地景正式美術、可用部署區與火力覆蓋平衡；不要把本原型當成最終美術交付，不擅自新增兵種或進入裝備階段。

## 最新：0.49.0 Camera 原型（後續以此節為準）

- 使用者「好繼續處理」後，實作鏡頭第一版，不重做已完成HUD，不把士兵／裝備變更混入。
- 新增 BattlefieldCamera.js、tests/td-camera.test.js；更新TDGame（draw、canvasPoint、reset、selectHero）、main（接工具列）、td.html、td-combat.css、package與sw、操作文件。
- 桌面／手機橫向Canvas現在匹配視窗長寬比，backing DPR上限1.5；原世界仍720×720。所有場上物件、範圍、特效一起套鏡頭；flash訊息單獨在螢幕上顯示。
- 查看模式／中鍵／空白＋左鍵拖動不發建造或單位命令；取消／失焦／resize清除手勢。全圖留白不接受部署／移動。回英雄、跟隨與選建造卡可退出查看。直式保留原呈現。
- 218／218自動測試、npm run check通過。瀏覽器測得Canvas1366×767及845×390而非正方形；移動鏡頭、放大後建塔與選取成功（240→140G、5→3木），全圖／定位／跟隨／橫向查看可用。發現並修復Grid撐高Canvas與全圖不能回到細節的問題。
- 待驗：實體手機觸控／長按、中鍵與空白拖曳真實鍵鼠、Edge、長局FPS。內嵌瀏覽器原80%縮放截圖裁切問題仍在，不宣稱完整像素視覺QA通過。
- **尚未完成新大地圖**：地圖素材、世界尺寸、Path、導航、可部署面積與數值都沒改；下一步才規劃新地圖構圖與逐步移除720硬編碼，先保護原路線行進時間與射程／放置測試。不要把鏡頭原型說成新地圖交付。
- 0.49.0僅本機，未commit／未推送GitHub。不要reset或discard包含多輪成果的dirty tree。

## 最新續作：0.48.0 邊緣 HUD V1（2026-09-14）

使用者要求先看介面，因此在已完成 P2/P3/P4/Audit 之上增加低風險呈現層，不是重做前述階段，也不是進入 P5。

- 修改：td.html、main.js、package.json、sw.js；新增 td-combat.css、tests/td-edge-ui.test.js；操作與維護文件同步。
- 已完成：PC／手機橫向四角 HUD、英雄能力折疊、原命令格按需顯示、原選取／军械庫折疊、建造卡圖片加大；直向沿用原面板，30 張建造按鈕不複製。
- 既有戰鬥、720×720 邏輯世界、地圖、AI、傷害、裝備數量規則完全沒有因本輪 UI 修改。
- UI 測試新增2項；最終212／212通過，npm run check、main.js語法檢查與git diff --check通過。
- 瀏覽器：1920×1080、約1366×768、845×390、390×845 CSS viewport 的 DOM 邊界／正方形 Canvas／控件驗證；弩塔放置扣款、商店、命令、召喚冷卻與倍速按鈕正常。Console 未見 error。內嵌瀏覽器80%缩放截圖被裁切，不能據此宣稱完整像素 QA 通過；真機觸控、Edge、長時間效能仍待測。
- **使用者最新方向**：接受重新設計更大的地圖，讓玩家移動鏡頭查看位置。這解除了先前「世界必須永遠720×720」的限制，但本輪沒有實作鏡頭。不要把外圍背景當成已經擴大的戰場。
- 下一步：先驗證大地圖／Camera 小型原型（螢幕↔世界座標、滑鼠／觸控移動鏡頭、英雄跟隨、地圖邊界、UI 不攔截戰場輸入），再替換地圖構圖；保留本輪HUD。士兵固定部署／單件裝備規則另外分階段，不與鏡頭一次重構。
- 版本0.48.0僅本機，未commit／未推送GitHub。下方0.47.0記錄屬歷史基線。


此檔是續作索引，**Repository 現況與測試優先於此紀錄**。本輪沒有 reset／discard，也沒有推送 GitHub。工作目錄仍包含 v0.45–0.46 與後續階段的未提交變更；不能把 `git diff` 全部誤認為本輪新增。

## 本輪開始時的核對

1. 讀取當時的進度紀錄、Repository、`git status --short`，比對 `git diff --stat` 與關鍵檔案 diff。前次進度記到 P2 建造預覽／確認、207／207；P3 起尚未完成。
2. `npm test`、`npm run check`、`git diff --check` 已重新執行。最終為 **210／210 通過**、語法檢查通過、diff check exit 0。Git 有 LF→CRLF 提示，未發現空白錯誤。
3. 沒有改 Wave、Economy、Tower balance、怪物數值、Hero 攻擊數值、Canvas 邏輯尺寸或地圖路線。

## 執行順序與實際狀態

| 順序 | 狀態 | 完成內容與驗證 |
| --- | --- | --- |
| P2 UX Revision | **完成；自動測試＋PC 瀏覽器已驗證** | 既有 `BuildSystem.queue/stagePlacement/confirmPlacement/placeQueued` 沿用。卡片選定後在戰場跟隨指標顯示原單位半透明預覽、射程與紅綠合法區；綠色位置**單擊即建造／扣資源**，無效位置不扣款；右鍵／Esc／取消可離開。移除多一步確認按鈕。測試涵蓋合法單擊。 |
| P3 Hero Skill 防無意義空放 | **完成；自動測試通過** | 檢查三英雄 Q/W/E/F：需要敵人的攻擊技能原本多數已拒絕空放；補 `Hero.castNova()` 銀葉 Q 只在範圍內有有效敵人時才施放及進 CD。陷阱、毒霧、召喚等預先部署能力仍可空場施放。修正舊測試的目標情境，新增兩個技能測試。 |
| P4 Tower／CombatUnit 可讀性 | **完成；自動測試通過** | 既有 14 塔的 `role` 沿用，替第一批三種守軍補 `role`；其餘單位已有定位文字。30 張建造卡的第一層只放名稱、短定位、金／木成本；完整攻擊、射程、攻速、單位 HP／護甲或塔既有技能說明放第二層。原卡片 click 購買流程不變。 |
| Build Drawer Presentation | **完成程式與自動測試；手機觸控驗收未完成** | PC Drawer 置中限寬 960px、水平捲動卡片；手機橫式縮至 160px 內並放大卡片文字；分類與最近使用沿用。桌面右鍵、觸控長按可顯示詳細資訊，點卡後回戰場放置。PC 實機瀏覽器驗證卡片、建造與 Console。橫式模擬視窗以手動 mobile 模式確認整頁無溢出；**未完成真機長按與極矮視窗 Drawer 目視 QA**。 |
| Battlefield／Camera／Canvas Audit | **完成靜態分析；未實作原型** | 見 [Audit](BATTLEFIELD_CAMERA_CANVAS_AUDIT.md)。確認 720×720 為實際世界座標，無 Camera；路徑、建造邊界、導航、技能、投射物與美術比例均有耦合。區分低風險 UI、中風險 Layout／Rendering／Input、高風險 World／Path／Camera／Map；列出四種路線與概念圖九個直接結論。 |
| P5 以後 | **未開始，按要求停止** | 未進入新的敵人、地圖、戰鬥系統或 P5。 |

## 修改檔案與內容歸屬

- 本輪 P2/P3/P4/Drawer 直接涉及：`src/td/TDGame.js`、`src/td/entities/Hero.js`、`src/td/config.js`、`src/td/main.js`、`td.html`、`td.css`、`tests/td-ui-flow.test.js`、`tests/td-core.test.js`。
- 新增分析／續作文件：`docs/BATTLEFIELD_CAMERA_CANVAS_AUDIT.md`、本檔。操作與維護文件的舊建造確認說明需同步更新。
- `src/td/systems/BuildSystem.js`、`LayoutSystem.js`、塔／裝備／戰報／掉落檔、`package.json`、`sw.js` 與其他文件原本已為未提交狀態（包含先前 P0–P2 與其他變更）；勿整檔覆蓋或回退。

## 測試與已知限制

- 最終 210／210、`npm run check` 與 `git diff --check` 通過。PC 瀏覽器看見完整美術、Drawer、所屬建造卡；王國弓塔合法點位建成、Drawer 自動收合、資源只在建成後扣除，Console 無 error。瀏覽器測試曾碰到本機伺服器未啟動造成素材載入失敗；啟動後重新載入正常，並非正式資源缺檔。
- 透過視窗模擬約 845×390 並手動切至 mobile/landscape，沒有整頁水平或垂直溢出；但此環境 `coarse=false`，自動偵測不等於真手機。**真實手機點擊／長按、安全區、極矮視窗 Drawer、Edge、30 波長時間與大量特效效能仍未驗證。** 不應宣稱 P4 的所有 UX 風險已消除。
- Canvas 仍固定 720×720；寬螢幕戰場容器周邊背景是裝飾，不是擴充的可玩世界。任何鏡頭或寬地圖工程必須先獨立原型、保護座標與平衡；此輪不做。
- 目前 `package.json` 仍是 0.47.0，代表本機待發布基線；P2 UX Revision／P3／P4 是未發布工作，未另建 commit 或升版。不要依版本字串推論 GitHub 已有此內容。

## 下次接續方式

1. 先讀本檔與 Audit，再執行 `git status`、檢查 diff、`npm test`、`npm run check`；不要 reset 任何未提交修改。
2. 若是本輪剩餘驗收，優先做真手機橫式 Drawer／長按與 Edge 目視測試；發現實際 bug 再做最小修復並重新測試。
3. **不要自行進 P5。** Camera／寬地圖只依玩家下一次人工確認的方向建立可撤回原型；概念圖僅作方向參考。
